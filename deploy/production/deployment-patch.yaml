apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-reactor
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: event-reactor
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
